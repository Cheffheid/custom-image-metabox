=== Image Metabox ===

A metabox that uses the WP media uploader to allow you to select an image.

TODO: Abstract out the saving of the metafield, so that adding the metabox to CPTs will actually save it too.
